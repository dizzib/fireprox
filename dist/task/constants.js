// Generated by LiveScript 1.2.0
(function(){
  var Assert, Shell, ROOT, DIRNAME, DIR, i$, len$, dir;
  Assert = require('assert');
  Shell = require('shelljs/global');
  ROOT = pwd();
  DIRNAME = {
    DIST: 'dist',
    SITE: 'site',
    TASK: 'task',
    TEST: 'test'
  };
  DIR = {
    DIST: ROOT + "/" + DIRNAME.DIST,
    dist: {
      SITE: ROOT + "/" + DIRNAME.DIST + "/" + DIRNAME.SITE
    },
    ROOT: ROOT + "",
    SITE: ROOT + "/" + DIRNAME.SITE,
    TASK: ROOT + "/" + DIRNAME.TASK,
    TEST: ROOT + "/" + DIRNAME.TEST
  };
  for (i$ = 0, len$ = DIR.length; i$ < len$; ++i$) {
    dir = DIR[i$];
    Assert(test('-e', dir));
  }
  module.exports = {
    dirname: DIRNAME,
    dir: DIR
  };
}).call(this);
